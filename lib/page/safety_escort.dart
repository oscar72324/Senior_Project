import 'package:flutter/material.dart';

class safetyEscort extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Saftey Escort'),
          centerTitle: true,
          backgroundColor: Colors.red,
        ),
      );
}
